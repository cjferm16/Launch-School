def add_three(number)
  return number + 3 #here, the program will return 7 because we are explicitly commanding it to return this line
  number + 4
end

returnedValue = add_three(4)
puts returnedValue